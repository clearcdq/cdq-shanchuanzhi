# 附件3：线缆测试标准与验收规范

## 3.1 测试总要求

所有新敷设的线缆（Cat6A网线、单模光纤）必须经过100%测试，并出具详细测试报告，测试指标全部达标后方可进行下一道工序。测试不合格的线缆必须返工。

---

## 3.2 Cat6A屏蔽网线测试标准

### 3.2.1 测试参数表（依据TIA/EIA-568-C.2 Category 6A）

| 测试参数 | 频率 | 标准限值 | 测试方法 | 合格判定 |
|----------|------|----------|----------|----------|
| 插入损耗 (IL) | 1-500MHz | ≤(1.9√f+0.01f+0.23)/100 | 时域反射 | ≤标准值 |
| 近端串扰 (NEXT) | 1-500MHz | ≥55-47log10(f/100) dB | 平衡测量 | ≥标准值 |
| 远端串扰 (FEXT) | 1-500MHz | ≥44-20log10(f/100) dB | 平衡测量 | ≥标准值 |
| 功率和串扰 (PSNEXT) | 1-500MHz | ≥52-47log10(f/100) dB | 计算值 | ≥标准值 |
| 衰减串扰比 (ACR) | 1-500MHz | ≥(NEXT-IL) dB | 计算值 | 正值，≥1dB |
| 等远端串扰 (ELFEXT) | 1-500MHz | ≥31-20log10(f/100) dB | 计算值 | ≥标准值 |
| 功率等远端串扰 (PSELFEXT) | 1-500MHz | ≥28-20log10(f/100) dB | 计算值 | ≥标准值 |
| 回波损耗 (RL) | 1-500MHz | ≥(18+5log10(f/100)) dB | 反射测量 | ≥标准值 |
| 传播延迟 | 1-500MHz | ≤570ns@100m | 时域测量 | ≤标准值 |
| 延迟偏差 | 1-500MHz | ≤50ns | 差分测量 | ≤标准值 |

**测试频率点：** 100MHz, 250MHz, 500MHz

### 3.2.2 测试要求
- 测试长度：实际敷设长度 + 两端跳线（总长≤120米）
- 测试端接：两端均为Cat6A屏蔽RJ45（带金属壳）
- 屏蔽连续性：屏蔽层两端可靠接地，测量屏蔽衰减≥20dB
- 测试平台：Fluke DSX-8000或同等精度仪器
- 测试报告：生成PDF报告，包含每条线缆的完整参数曲线图

### 3.2.3 合格标准
- **所有参数**必须满足或优于TIA Cat6A标准限值
- **裕量要求**：关键参数（IL、NEXT、RL）需有≥3dB余量
- **一票否决**：任一参数不达标，整条线缆判定不合格

---

## 3.3 单模光纤测试标准

### 3.3.1 测试参数表（依据ISO/IEC 11801 Ed2.0）

| 测试参数 | 标准限值 | 测试方法 | 测试波长 |
|----------|----------|----------|----------|
| 衰减系数 | ≤0.35dB/km@1550nm<br>≤0.40dB/km@1310nm | 插入损耗法 | 1310nm/1550nm |
| 回波损耗 | ≥50dB (连接器) | OTDR/功率计 | 1310nm/1550nm |
| 长度 | 实际长度±1m | OTDR测量 | - |
| 熔接点损耗 | ≤0.1dB/点 | OTDR曲线分析 | - |
| 连接器损耗 | ≤0.3dB/个 | 插回损测试 | - |
| 总链路损耗 | ≤(长度×衰减系数+连接器损耗) | 功率计测试 | - |

### 3.3.2 测试要求
- **OTDR测试**：每段光纤必须用OTDR测试曲线，记录熔接点位置和损耗
- **功率计测试**：两端用光功率计测试实际插入损耗，验证总损耗
- **连接器清洁度**：每个LC连接器必须用专业清洁工具清洁后测试
- **两端测试**：A→B和B→A双向测试，取平均值

### 3.3.3 合格标准
- **衰减系数**：实测值≤标准限值的90%
- **熔接点**：每点损耗≤0.1dB，曲线无异常反射峰
- **连接器**：回波损耗≥50dB，插入损耗≤0.3dB
- **总损耗**：实测值比理论值高≤0.5dB

---

## 3.4 测试流程与验收文档

### 3.4.1 测试流程
```
1. 线缆敷设完成 → 2. 两端端接（配线架/设备） → 3. 标签标识
   ↓
4. Fluke测试（Cat6A）/ OTDR测试（光纤）
   ↓
5. 不合格？ → 返工 → 重新测试
   ↓
6. 合格 → 生成测试报告 → 提交验收
   ↓
7. 信息科签字确认 → 进入下一工序
```

### 3.4.2 验收文档清单
- [ ] **Cat6A线缆测试报告**（PDF，含每条线缆的10项参数）
- [ ] **单模光纤OTDR曲线图**（PDF，标注熔接点位置）
- **汇总表**：
  - 线缆编号、敷设路径、长度、测试日期
  - 测试结果（合格/不合格）
  - 测试人员签字
  - 施工单位盖章

### 3.4.3 质量保留期
所有测试报告和原始数据电子档需长期保存（≥10年），作为工程档案备查。

---

## 3.5 测试不合格处理流程

1. **记录问题**：详细记录不合格参数、线缆编号、位置
2. **原因分析**：施工方分析原因（端接工艺、线缆损伤、设备问题）
3. **制定方案**：提出整改方案（重端接、更换线缆、检查设备）
4. **实施整改**：施工方执行，整改后重新测试
5. **复测确认**：复测合格后方可通过
6. **责任追溯**：记录整改过程，作为质量考核依据

**原则：** 不达标线缆绝不放过，必须达到100%合格率。

---

**编制：** 网络工程师  
**审核：** 信息科  
**日期：** 2026年3月

**参考标准：**
- TIA/EIA-568-C.2 《平衡双绞线电信布线标准》
- ISO/IEC 11801 Ed2.0 《信息技术 - 通用布线规范》
- GB/T 50312-2016 《综合布线系统工程验收规范》