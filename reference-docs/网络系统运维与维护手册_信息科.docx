# PACS网络系统运维与维护手册
**（信息科网络管理员专用）**

---

## 一、系统架构总览

### 1.1 网络拓扑摘要

```
                 ┌─────────────────────────────────────┐
                 │        医院核心网络                 │
                 │  ┌─────────────┐   ┌─────────────┐ │
                 │  │ Core-Switch1│   │ Core-Switch2│ │
                 │  │ (H3C S6800) │   │ (H3C S6800) │ │
                 │  └──────┬──────┘   └──────┬──────┘ │
                 │         │ LACP/VRRP       │        │
                 │    ┌────▼──────────────────▼────┐   │
                 │    │   PACS服务器群（新院区机房）  │   │
                 │    └────────────────────────────┘   │
                 └─────────────────────────────────────┘
                           │                        │
               ┌───────────▼───────────┐ ┌─────────▼──────────┐
               │  新院区接入层（3台）   │ │ 老院区接入层（2台）│
               │ Switch-New-01/02/03  │ │ Switch-Old-01/02  │
               └───────────┬───────────┘ └─────────┬──────────┘
                           │                        │
                    ┌──────▼──────┐           ┌────▼──────┐
                    │ 工作站×15台 │           │ 工作站×15台│
                    │ 10G网卡     │           │ 10G网卡    │
                    └─────────────┘           └───────────┘
```

### 1.2 VLAN规划表

| VLAN ID | 名称 | 子网 | 用途 | 网关 |
|---------|------|------|------|------|
| 10 | PACS-Servers | 10.10.1.0/24 | PACS服务器、存储 | 10.10.1.1 |
| 20 | Radiology-New | 10.10.20.0/24 | 新院区放射科终端 | 10.10.20.1 |
| 30 | Radiology-Old | 10.10.30.0/24 | 老院区放射科终端 | 10.10.30.1 |
| 99 | Management | 10.10.99.0/24 | 网络设备管理 | 10.10.99.1 |

**核心接口配置：** 所有上行接口均为trunk，允许VLAN 10/20/30/99通过

---

## 二、日常巡检清单（每日/每周）

### 2.1 每日必查（5分钟）

| 检查项 | 命令/操作 | 正常标准 | 异常处理 |
|--------|-----------|----------|----------|
| 交换机状态 | `display device` | 所有板卡Power/Status=OK | 异常立即排查 |
| 接口流量 | `display interface brief` | 无error/overrun持续增长 | 检查端口错误 |
| CPU/内存 | `display cpu-usage`<br>`display memory-usage` | CPU<30%，Mem<60% | >70%需分析 |
| PACS连通性 | ping 10.10.1.100（PACS服务器） | 延迟<10ms，0%丢包 | 持续丢包检查链路 |
| 日志告警 | 查看网管系统/交换机日志 | 无CRITICAL/ERROR | 如有告警立即处理 |

### 2.2 每周检查（15分钟）

- [ ] 查看核心交换机堆叠状态：`display irf` → 确保拓扑为"*"
- [ ] 检查LACP聚合组：`display link-aggregation summary` → 所有聚合组Up
- [ ] 检查STP根桥：`display stp brief` → 核心应为根桥
- [ ] 检查ARP表异常：`display arp | include Dynamic` → 无异常增长
- [ ] 检查MAC表稳定性：`display mac-address` → 无频繁刷新
- [ ] 备份配置：`tftp 10.10.99.200 put VRPcfg.cc` → 每周至少1次

---

## 三、典型故障处理

### 3.1 单个工作站无法访问PACS

**现象：** 某台电脑打不开PACS，其他正常

**排查流程：**
```
1. 确认电脑IP是否正确（应在VLAN20或30）
   → 错误：检查DHCP/静态配置

2. ping本机网关（10.10.20.1或30.1）
   → 不通：检查交换机端口、线缆
   → 通：继续

3. ping PACS服务器（10.10.1.100）
   → 不通：检查VLAN间路由、ACL
   → 通：检查PACS服务器/客户端软件

4. 检查交换机端口状态：
   display interface Gi 1/0/XX
   → 查看错误计数（error/overrun/drop）
   → 如有错误，可能是线缆或网卡问题
```

**快速恢复：** 更换网线 → 更换交换机端口 → 更换网卡

---

### 3.2 多个终端同时卡顿

**现象：** 某区域（如新院区3楼）所有终端调阅缓慢

**可能原因：**
1. **上行链路拥塞**：核心交换机某个端口流量打满
2. **QoS策略失效**：流量未正确标记或调度
3. **PACS服务器负载高**：存储响应慢
4. **广播风暴/环路**：MAC表频繁抖动

**排查：**
```
1. 查看核心交换机上行端口流量：
   display interface Te 1/0/1
   → 观察in/out rate，是否>8Gbps（10G端口）

2. 检查QoS策略：
   display qos policy interface
   → 确认pacs-policy已应用

3. 抓包分析（如怀疑环路）：
   [在接入交换机抓包]
   Wireshark查看是否有大量ARP/广播

4. 检查服务器性能：
   登录PACS服务器，查看CPU/内存/磁盘IO
```

**解决：**
- 链路拥塞：检查是否有非PACS流量占满（如备份），临时限速
- QoS失效：重新应用策略或重启端口
- 环路：找到环路端口并断开（Spanning Tree应能阻止，但需排查根源）

---

### 3.3 链路冗余切换不成功

**现象：** 拔掉一条上行光纤，业务中断超过30秒

**检查：**
```
1. 确认LACP聚合组正常：
   display link-aggregation summary
   → Actor/Partner状态应为Selected

2. 确认STP无阻塞：
   display stp brief
   → 聚合端口不应被阻塞

3. 检查VRRP（如有）：
   display vrrp verbose
   → Master状态应能切换

4. 检查ARP表是否老化：
   大流量拥塞时，ARP老化时间可适当缩短至30秒
```

**解决：**
- 调整LACP参数（快速超时）
- 缩短ARP老化时间
- 检查光纤模块是否有误码（更换模块）

---

### 3.4 线缆测试不合格

**场景：** Fluke测试显示某条Cat6A线缆NEXT或RL不达标

**处理：**
1. **复测**：可能是测试误差，换仪器再测
2. **检查两端端接**：RJ45水晶头是否按照T568B标准？屏蔽层是否可靠接地？
3. **检查线缆破损**：弯曲半径是否过小？有无挤压痕迹？
4. **更换**：如确认线缆本身问题，整段更换（通常5-10米局部更换）

**标准：** 必须100%达标，不允许有容忍余量

---

## 四、性能监控与调优

### 4.1 关键性能指标（KPI）

| 指标 | 采集方法 | 阈值 | 告警值 |
|------|----------|------|--------|
| 核心端口利用率 | snmp/ifHCInOctets | <70% | >85% |
| 接入端口错误率 | display interface | 0 | 持续增长 |
| PACS端到端延迟 | ping -c 100 10.10.1.100 | <10ms | >20ms |
| 丢包率（高峰） | iperf3测试 | 0% | >0.5% |
| 交换机CPU | display cpu-usage | <30% | >70% |
| 交换机内存 | display memory | <60% | >80% |

### 4.2 调优手段

**场景：高峰时段仍有卡顿**
1. **细化QoS**：在接入层也应用QoS策略，早早在边缘分类
2. **端口限速**：对非PACS端口限速至100Mbps，防止单个终端占满
3. **流量清洗**：检查是否有终端中毒（如ARP flood），隔离异常端口
4. **增加带宽**：如确实需求增长，将上行升级至40G（需更换模块）

---

## 五、配置备份与恢复

### 5.1 日常备份
```text
# 手动备份到TFTP服务器
tftp 10.10.99.200 put VRPcfg.cc Switch-New-01.cfg

# 自动备份脚本示例（cron每天凌晨2点）
0 2 * * * /home/admin/backup_config.sh
```

### 5.2 快速回滚
**场景：** 配置错误导致业务中断

**步骤：**
1. 通过console口连接设备（如果网络不可达）
2. 清除错误配置（使用`undo`命令或直接恢复）
3. 或重启时指定旧配置启动（需保留多个配置）
```text
# 查看启动配置和备份配置
display startup
# 指定下次启动使用备份配置
startup saved-configuration backup.cfg
reboot
```

---

## 六、固件升级规范

### 6.1 升级前检查
- [ ] 备份当前配置（主+备）
- [ ] 准备回滚方案（保留旧版本软件）
- [ ] 确认升级窗口（非诊疗高峰，如周末凌晨）
- [ ] 通知放射科（计划中断时间）

### 6.2 升级步骤
1. 上传新版本到设备：`ftp 10.10.99.200 put S6800V700R001C00.cc`
2. 指定启动文件：`startup system-software S6800V700R001C00.cc`
3. 保存配置：`save`
4. 重启：`reboot`（注意：双机需逐一重启，避免全阻）
5. 验证：`display version`，确认版本号；验证业务

### 6.3 回滚（如升级失败）
```text
# 重启时选择旧版本
reboot
# 在boot menu选择backup image
```

---

## 七、文档与记录

### 必须维护的文档
1. **网络拓扑图**（最新版Visio，包含设备IP、VLAN）
2. **IP地址分配表**（Excel，记录每个端口对应）
3. **配置备份**（按设备分目录，每周更新）
4. **故障记录表**（每次故障原因、处理、耗时）
5. **变更日志**（任何配置变更记录）
6. **设备资产清单**（序列号、购买日期、质保到期）

### 文档位置
- 主目录：`/home/kali/桌面/网络项目资料/`
- 子目录：`configs/` `diagrams/` `logs/` `assets/`

---

## 八、应急预案（快速参考）

### 8.1 核心交换机完全宕机
**响应时间：** 10分钟内

**步骤：**
1. 确认物理状态（电源、风扇）
2. 如硬件故障，启用备机（如有）或启用旧设备临时替换
3. 核心层双机热备应自动切换，若无，手动配置备用交换机上电
4. 逐级恢复：核心→接入→终端
5. 全程记录，事后写故障报告

### 8.2 院区间光纤中断
**响应时间：** 30分钟内

**步骤：**
1. 联系运营商确认光缆故障
2. 启用5G CPE应急链路（如有备用方案）
3. 临时调整路由策略，优先保障新院区访问老院区PACS（或反向）
4. 通知放射科可能延迟，请错峰使用
5. 跟踪运营商修复进度

### 8.3 大规模病毒感染/攻击
**响应：**
1. 隔离受影响网段（VLAN间ACL阻断）
2. 分析攻击源（MAC/IP溯源）
3. 清除病毒/打补丁
4. 逐台恢复
5. 事后加固（端口安全、802.1X）

---

## 九、联系方式

**设备厂商支持：**
- H3C 400-600-9900（7×24）
- Chelsio 网卡驱动：developer.chelsio.com

**施工方紧急联络：** [填写施工方电话]

**院内协调：**
- 放射科主任：______
- 信息科科长：______

---

**本手册版本：** V1.0  
**最后更新：** 2026年3月15日  
**适用系统：** H3C S6800系列 + PACS网络

---

**编制：** 网络工程师  
**审核：** 信息科主任

**重要：** 所有运维操作必须填写《网络变更工单》，重大变更需领导审批。